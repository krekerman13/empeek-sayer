import React from 'react';
import PropTypes from 'prop-types';
import Header from '../Header/Header';
const List = props => {
    return (
        <div>
            <Header />
            <p>List</p>
        </div>
    );
};

export default List;
