import React from 'react';
import PropTypes from 'prop-types';
import Header from '../Header/Header';
const List = props => {
    return (
        <div>
            <Header />
        </div>
    );
};

export default List;
